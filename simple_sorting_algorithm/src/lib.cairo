/// Sorts a list of integers using bubble sort, returning the sorted array.
/// Built with Cairo 2.10.1 as an executable, supports STARK proof generation via `scarb prove`.
/// Tests validate sorting for empty, single-element, and multi-element arrays, including
/// duplicates.

use core::array::ArrayTrait;
use core::array::SpanTrait;
